# If you deploy the backend somewhere, you can point the frontend directly:
# VITE_API_BASE_URL=https://your-backend.com/api

# Dev proxy target (only used by vite.config.ts):
VITE_API_PROXY_TARGET=http://localhost:8082
